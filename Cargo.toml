[workspace]
members = [
    "node",
    "pallets/subspace",
    "runtime",
    "calls",
    "relayer"
]

[profile.release]
panic = "unwind"

[profile.test]
opt-level = 3

# [patch.crates-io]
# zstd = { git = "https://github.com/gyscos/zstd-rs.git", tag = "v0.13.0" }
